(ns gcp.storage.v2.Storage.BucketGetOption
  (:import (com.google.cloud.storage Storage$BucketGetOption)))

(defn ^Storage$BucketGetOption from-edn [arg]
  (throw (Exception. "unimplemented")))

(defn to-edn [^Storage$BucketGetOption arg]
  (throw (Exception. "unimplemented")))