(ns gcp.bigquery.v2.BigQuery.RoutineListOption
  (:import (com.google.cloud.bigquery BigQuery$RoutineListOption)))

(defn ^BigQuery$RoutineListOption from-edn [arg] (throw (Exception. "unimplemented")))

(defn to-edn [^BigQuery$RoutineListOption arg] (throw (Exception. "unimplemented")))