(ns gcp.bigquery.v2.ParquetOptions
  (:import [com.google.cloud.bigquery ParquetOptions])
  (:require [gcp.global :as g]))

(defn ^ParquetOptions from-edn [arg] (throw (Exception. "unimplemented")))

(defn to-edn [^ParquetOptions arg] (throw (Exception. "unimplemented")))