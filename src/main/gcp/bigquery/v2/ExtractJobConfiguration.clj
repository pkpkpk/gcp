(ns gcp.bigquery.v2.ExtractJobConfiguration
  (:import (com.google.cloud.bigquery ExtractJobConfiguration)))

(defn from-edn [arg] (throw (Exception. "unimplemented")))

(defn to-edn [^ExtractJobConfiguration arg] (throw (Exception. "unimplemented")))