(ns gcp.storage.v2.Storage.BucketTargetOption
  (:import (com.google.cloud.storage Storage$BucketTargetOption)))

(defn ^Storage$BucketTargetOption from-edn
  [arg]
  (throw (Exception. "unimplemented")))

(defn to-edn
  [^Storage$BucketTargetOption arg]
  (throw (Exception. "unimplemented")))