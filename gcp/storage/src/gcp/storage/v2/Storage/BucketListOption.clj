(ns gcp.storage.v2.Storage.BucketListOption
  (:import (com.google.cloud.storage Storage$BucketListOption)))

(defn ^Storage$BucketListOption from-edn
  [arg]
  (throw (Exception. "unimplemented")))

(defn to-edn
  [^Storage$BucketListOption arg]
  (throw (Exception. "unimplemented")))